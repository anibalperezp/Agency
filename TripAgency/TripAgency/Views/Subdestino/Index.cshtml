@model PagedList.IPagedList<TripAgency.EF.Subdestino>
@using PagedList.Mvc;
@Styles.Render("~/Content/pagedlist")
@Scripts.Render("~/bundles/new")

@{
    ViewBag.Title = "Subdestino";
}
<h2>Subdestino</h2>

<p>
    @Html.ActionLink("Añdair nuevo", "Create")
</p>

<br />
<div class="row">
    <div class="col-md-3">
        <form action="#" method="get">
            <div class="input-group">
                <!-- USE TWITTER TYPEAHEAD JSON WITH API TO SEARCH -->
                <input class="form-control" id="system-search" name="Search_Data" placeholder="Buscar por" required="">
                <span class="input-group-btn">
                    <button type="submit" class="btn btn-default">
                        <i class="glyphicon glyphicon-search"></i>
                    </button>
                </span>
            </div>
        </form>
    </div>
    <div class="col-md-9">
        <table class="table table-hover table-list-search">
            <tr>
                <th>
                    @Html.ActionLink("Destino", "Index", new { Sorting_Order = ViewBag.SortingDestino })
                </th>
                <th>
                    @Html.DisplayName("Nombre")
                </th>
                <th>
                    @Html.DisplayName("Imagen")
                </th>
                <th>
                    @Html.DisplayName("Imagen")
                </th>
                <th>
                    @Html.DisplayName("Descripción")
                </th>

                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Destino.destino1)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.subdestino1)
                    </td>
                    <td>
                        <img height="50" src=@item.foto_sub1 width="50" alt=@item.foto_sub1 />
                    </td>
                    <td>
                        <img height="50" src=@item.foto_sub2 width="50" alt=@item.foto_sub2 />
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.descripcion_subdestino)
                    </td>


                    <td>
                        @Html.ActionLink("Editar", "Edit", new { id = item.idSubdestino })
                        @Html.ActionLink("Borrar", "Delete", new { id = item.idSubdestino })
                    </td>
                </tr>
            }
        </table>
        Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount
        @Html.PagedListPager(Model, Page_No => Url.Action("Index", new { Page_No, Sorting_Order = ViewBag.CurrentSortOrder, Filter_Value = ViewBag.FilterValue }))
    </div>
</div>